<!DOCTYPE html>
<html>
<head>
    <title>Todo List</title>
</head>
<body>
    <h1>Todo List</h1>
    <ul>
        <% todos.forEach(todo => { %>
            <li>
                <%= todo.text %> - <%= todo.done ? 'Completed' : 'Pending' %>
            </li>
        <% }); %>
    </ul>
</body>
</html>
